services:
  db:
    image: mysql:8
    ports:
      - 3306:3306
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}

  app:
    depends_on: ['db']
    image: ev-server
    ports:
      - 3000:3000
